Git >> Version Control
   V1
   v2
   v3
   v4 now...


   >> It is used to distributed code (cloud)
   >> Collabration 
   >> Tracking Changes
   >> Recovery

  # For first time 
   >>  Run this command >> git config --global user.name "profileShravan"
                           git config --global email.name "shravanbathri.8000@gmail.com"  
                           git config --global color.ui auto

    >> Mandatory >> git init 
                    git remote add orign repositoryaddress      
                    git pull orign branchname    [Pull a file from git]
                    git status

                    git add filename             [Push a file from local to git]
                    git status
                    git commit -m "Commit Message"
                    git push


    >> Create new Branch >> git checkout -b "branchname"              